# Assessment OTP Invite (account-less flow)

## What it is

A per-campaign mode for **corporate-assigned** assessments where the candidate does **not** need a PluginLive student account. They receive a single email containing an invite link, verify their email with a 6-digit OTP, and drop straight into the assessment runner.

Originally built for AI Interview. Generalized incrementally to all corporate-side assessment types: AI Interview, Communication, Aptitude, Hinglish, Custom (2026-06-10/11), then **Role-Based + Behaviour (2026-06-15)**. Each type is wired independently in its own `assign*Assessment` method — see Backend touchpoints.

Institute-side assessments still go through the portal-login flow.

## Admin trigger

`admin-react` Assessment Assignment screen has a corporate-only delivery checkbox. **As of 2026-06-11 the default is reversed: OTP invite is now the DEFAULT for corporate assignments.** The checkbox reads **"Send email & password instead (portal login, no OTP)"** — leave it **unticked (default)** for the no-login OTP invite (`is_otp_invite = true`); **tick it** to fall back to the legacy portal-signup + email/password reminder flow (`is_otp_invite = false`). Implementation: state `isOtpInvite` defaults to `true`, is reset to `true` whenever the entity switches to corporate (`false` for college), the checkbox is rendered `checked={!isOtpInvite}` / `onChange={e => setIsOtpInvite(!e.target.checked)}`, and the payload still sends `isOtpInvite` (true = OTP). The flag is round-trippable so resends and post-hoc listings stay consistent.

## Data model

### Postgres — single column

```sql
ALTER TABLE assessment.assessment_corporate_map
  ADD COLUMN is_otp_invite BOOLEAN NOT NULL DEFAULT false;
```

- One row per corporate campaign; the flag lives at campaign granularity, not per-student.
- `DEFAULT false` → zero backfill, every existing campaign keeps its legacy behavior.
- No new tables, no enum changes, no new indexes.
- DB-Scripts: `Assessment OTP Invite/001_corporate_map_is_otp_invite.sql`.

### Postgres — invite short links (2026-07-15)

```sql
CREATE TABLE assessment.assessment_invite_short_links (
  code                    VARCHAR(9)  PRIMARY KEY,
  assessment_assigned_id  UUID        NOT NULL UNIQUE REFERENCES assessment.assessment_assigned_students ON DELETE CASCADE,
  expires_at              TIMESTAMPTZ NOT NULL,
  revoked_at              TIMESTAMPTZ,
  click_count             INTEGER     NOT NULL DEFAULT 0,
  last_clicked_at         TIMESTAMPTZ,
  created_at              TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at              TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

- Stores **only** the assignment id — never candidate PII. The JWT payload is re-derived at click time.
- `assessment_assigned_id` UNIQUE = one code per assignment, so the invite email and admin Copy Link emit the identical URL.
- DB-Scripts: `Assessment OTP Invite/20260715T100351Z__assessment_invite_short_links.sql`. **DEV + UAT applied 2026-07-15; PROD applied 2026-07-18.**

### MongoDB (user-management-node) — generalized OTP store

- Old: `ai_interview_invite_otp` collection + `AiInterviewInviteOtp` model + `aiInterviewOtpHandler` + `/public/interview-otp/*` routes.
- New: `assessment_invite_otp` collection + `AssessmentInviteOtp` model + `assessmentInviteOtpHandler` + `/public/assessment-otp/*` routes.
- The old paths/models are **kept as thin aliases** for backward compatibility — already-issued AI Interview invite links keep working.
- The OTP record stores: hashed OTP, expiry, `assessment_assigned_id`, `assessment_type`, `email`, attempt counter, resend timestamp.

## Email flow

When `is_otp_invite = true` the admin-node assignment handler **suppresses both** the portal-signup email and the legacy "complete your assessment" reminder, and sends only the OTP invite-link email.

The invite-link email template lives at `user-management-node/src/utils/emailTemplates/assessmentInviteOtp.js` (renamed from the AI-interview-specific template). It carries: candidate name, assessment name (resolved live), corporate name, the invite link, expiry window.

Since 2026-07-15 the link handed out is the **short** `/s/<code>` form, not `/assessment/start/<jwt>` — see [Invite link shortener](#invite-link-shortener-scode-2026-07-15).

The same link can be copied manually from the admin assessment detail **StudentsTable** (Copy Link action) and shared via WhatsApp/SMS. This is a fallback for failed email delivery and reuses the same OTP-invite flow — the candidate still verifies email with a 6-digit OTP before entering the assessment. See `Assessment/admin-frontend.md` for the institute equivalent (activation link).

The invite URL is built from `process.env.ASSESSMENT_FE_BASE_URL` (admin-node helper `app/helpers/assessmentInviteEmail.js`). Each environment **must** set this — the helper falls back to `https://assessment.dev.pluginlive.com` if unset, so a UAT or PROD container without the var silently sends candidates to DEV. UAT value: `https://assessment.uat.pluginlive.com`. PROD value: `https://assessment.pluginlive.com`.

## SMS channel — OTP also texted via MSG91 (2026-07-09)

The **same** verification OTP is now also delivered by **SMS via MSG91**, as a **best-effort second channel** alongside the email. Email remains the source of truth; SMS never blocks or fails the request.

- **Where:** `user-management-node` `assessmentInviteOtpHandler.requestOtp` (and `/resend`). After the email is dispatched, it looks up the candidate's phone and texts the same code. `verifyOtp` is unchanged — one hash validates a code read from either channel.
- **Sender:** `user-management-node/app/helpers/msg91Sms.js` → `sendOtpSms({to, otp})` POSTs to MSG91's **Flow API** (`https://control.msg91.com/api/v5/flow/`) with `{ template_id, recipients:[{ mobiles, var: otp }] }`. Reuses the WhatsApp helper's `normalizeRecipient` (India +91). Reuses the existing `MSG91_AUTHKEY` (same MSG91 account `pluginlive5` as WhatsApp).
- **Phone source (2026-07-09):** captured on the **admin** side at candidate-add time — the account-less OTP candidate never has a user record, so a `users.phone_number` lookup finds nothing. An **optional "Mobile Number"** field was added to admin-react's Add-Candidate form and the bulk CSV (`mobile_number`/`mobile`/`phone`/`contact_number` headers) in `EnhancedBulkUploadDrawer.js`; the value rides on the candidate object into `studentsData`/`bulkUploadData` (`AssessmentSelect.js` maps carry `phone`). admin-node's `getAssessmentAssignedParticipants` (corporate branch) and the async job payload (`__phone`, alongside `__name`) thread it, and it is **minted into the invite JWT as a `phone` claim** by `mintInviteToken`/`buildInviteUrl`/`sendAssessmentInviteEmail` across **every** OTP-invite path (AI_Interview/Communication/Aptitude/Custom/Behavior/Role_Based — sync mint sites *and* the async `assignmentWorker` OTP branch). UMS `requestOtp` then reads `invite.phone` first, falling back to the `users.phone_number` lookup (encrypted, decrypted via `MASKING_SECRET`, matched case-insensitively by email) for older tokens/other flows. The candidate screen still asks for **email only** — no phone is entered there. Phone is **optional** end-to-end: absent → email-only, nothing breaks. No column added to `assessment_invite_otps`, no DB migration.
- **Phone is now persisted to the student profile (2026-07-10).** The entered mobile is written into the silent OTP candidate's `student.student_personal_profile.contact_number` at provisioning — the `admin.phoneNumber` in the `buildPayload`/`buildAiInterviewStudentPayload` provisioning payloads was hardcoded `""`; it now sources `student.phone`/`s.phone`/`cand.phone` (from `getAssessmentAssignedParticipants`, which carries `phone`). `createPublicStudent` (student-node) maps `admin.phoneNumber → studentPersonalProfile.phoneNumber → spp.contact_number`. Effects: (a) the assessment student list's **CONTACT DETAILS** column (reads `spp.contact_number`) now shows the number instead of `-`; (b) the number is resolvable by email for any downstream reader.
- **Copy Link now carries the phone for ALL assessment types (2026-07-10).** `getAssessmentInviteLink` (`Assessment.js`) LEFT JOINs `student.student_personal_profile` on `primary_email` and passes `phone: spp.contact_number` into `buildInviteUrl` → the copy-link token gets the `phone` claim, so SMS OTP fires for every type opened via Copy Link (previously only the assign-time email path — effectively Communication — carried it). NB: only applies to candidates whose profile has a phone (i.e. added via the mobile field going forward; ~8k pre-existing accounts already have `spp.contact_number`).
- **Admin UI (2026-07-10):** the Add-Candidate drawer shows the mobile in the candidate card list and a **Mobile** column in the candidate table (`EnhancedBulkUploadDrawer.js`).
- **Role_Based & Custom_Assessment did NOT get SMS (fixed 2026-07-13).** Their corporate-OTP branches explicitly **"do not create student accounts"**, so — unlike AI_Interview/Communication (which provision a silent profile) — there is **no `student_personal_profile`** row, so the profile-based phone lookup (Copy Link's `spp` join, CONTACT DETAILS) found nothing and SMS never fired via Copy Link. Fix: a nullable **`contact_number` column on `assessment.assessment_assigned_students`** (migration `Assessment OTP Invite/…__assigned_students_contact_number.sql`; Prisma `AssessmentAssignedStudent.contactNumber` in admin-node + student-node) is populated from `candidate.phone`/`student.phone` at the RB (`generateRoleBasedQuestions.js`) and Custom (`customAssessment.js`) assign create sites. `getAssessmentInviteLink` (Copy Link) and both student-list queries now read **`COALESCE(aas.contact_number, spp.contact_number)`** — so the phone resolves for profile-less types (RB/Custom via the row) and profile types (AI/Comm via `spp`) alike. Custom provisioning `phoneNumber` (was `""`) also now sources the candidate phone. DEV+UAT live 2026-07-13; only new assigns populate the column (no backfill).
- **Custom_Assessment is ASYNC — needed a separate fix (2026-07-13).** `Custom_Assessment` corporate is async-enabled (`AssignmentJobService.isAsyncEnabled` → true for Custom corporate), so its `assessment_assigned_students` rows are created by the **queue worker's `assignmentDb.insertAssignedStudent`**, NOT the sync `createMany` — so the sync-path `contact_number` populate never ran for Custom, and (being account-less/no profile) Copy Link/CONTACT DETAILS/SMS stayed empty. Fix: the Custom async job item now carries `__phone` (`customAssessment.js`), `insertAssignedStudent` accepts + inserts `contact_number`, and the worker's assign stage passes `item.studentPayload.__phone`. Now every async assign row (Custom + also AI/Comm/Aptitude) stores the mobile. Role_Based is unaffected — it uses its own set-group `prisma.create` path, not the queue worker.
- **Pre-fix rows can't be fully backfilled.** Role_Based saves its roster (with phone) in `student_lists.students_data`, so old RB rows were backfilled by email (`UPDATE … FROM jsonb_array_elements(students_data)`). Custom does NOT persist a roster, so a pre-fix Custom candidate's phone is unrecoverable — those need a fresh assign. New assigns populate `contact_number` automatically.
- **Institute phone display (2026-07-13):** `getAssessmentAssignedParticipants` only added `phone` on the corporate branch; the **institute branch dropped it**, so institute students' entered mobile never reached provisioning (`spp.contact_number`) or the async assign row — CONTACT DETAILS showed `-`. Added `phone` to the institute branch (parity with corporate). Institute assessments use portal login (not OTP), so this is purely the phone **display**; new institute assigns now store it. Pre-fix rows aren't backfillable (institute roster not persisted with phone).
- **Role_Based has its OWN async item-builder — was the actual live gap (fixed 2026-07-13).** DEV/UAT run with `ASSIGNMENT_ASYNC_ENABLED=1`, which routes **every** assessment type through the assignment queue (`isAsyncEnabled` returns true unconditionally). So the fix in `processStudentsForAssessment`'s sync `createMany` (the 2026-07-13 RB/Custom fix above) was **dead code for real assigns** — Role_Based's actual live path is `AssessmentSetGroupService.assignStudents`'s own async item-builder (separate from `customAssessment.js`'s), which builds `items`/`studentPayload` WITHOUT `__phone`. Only Custom_Assessment's async builder had `__phone` wired at that point, which is why RB and Custom appeared to alternate ("fix one, break the other") — they are two structurally-identical but entirely separate item-builders, and only one had the fix at a time. Added `__phone: stu.phone || stu.mobile_number || null` to the RB async item-builder (`AssessmentSetGroupService.js` ~line 1085) so the worker's `insertAssignedStudent` (already reading `item.studentPayload.__phone` since the Custom fix) stores `contact_number` for RB rows too.
- **Verify every `createJob` item-builder sets `__phone` when adding a new async assessment type.** As of 2026-07-13 all 6 call sites do: AI_Interview (`Assessment.js` ~18570), Communication/Behavior (`Assessment.js` ~5403/~6087/~9092), Custom_Assessment (`customAssessment.js` ~1084), Role_Based (`AssessmentSetGroupService.js` ~1095). A new async type or a new item-builder for an existing type is an easy place to silently drop this — grep `__phone` across all `createJob` callers before shipping.
- **Root cause of the RB/Custom flip-flop, fixed for good (2026-07-13, same day, follow-up).** The `contact_number`/`__phone` patches above (2026-07-13 entries) treated the *symptom* — RB/Custom never create a `student_personal_profile` row for corporate-OTP candidates, unlike every other type — and kept needing a parallel patch in each of RB's and Custom's **4 independent call sites** (sync + async × 2 types), which is why fixing one broke/missed the other. The actual fix: RB and Custom now **always provision a silent student profile for new OTP-corp candidates too**, exactly like AI_Interview/Communication/Aptitude/Behavior (`createPublicStudent` with `assessment_data.skipActivationEmail: true` — never a login email, only the scoped OTP invite is sent separately). Changed: `script/generateRoleBasedQuestions.js` (`processStudentsForAssessment` no longer returns early for `isCorporateOtpInvite` — falls through to STEP 2's account creation, reminder-email block guarded with `!isCorporateOtpInvite`), `app/service/AssessmentSetGroupService.js` (RB async item-builder: `provision = !isExisting && !isOtpCorp` → `provision = !isExisting`), `app/models/customAssessment.js` (sync `createStudentsIfNeeded`: dropped the `!isOtpInvite` guard on account creation, added `skipActivationEmail: isOtpInvite`; async item-builder: same `provision` fix as RB). The async worker's `buildUserData` already force-sets `skipActivationEmail: true` for every async-provisioned account and the OTP invite email was already independent of new/existing status in every path, so this is safe — verified by reading `assignmentWorker.js` before changing. Net effect: `spp.contact_number` (the same source every other type relies on) now populates for RB/Custom too; the `assessment_assigned_students.contact_number` column/`COALESCE` fallback from the earlier fix stays in place but is no longer load-bearing for new assigns. Deployed DEV (CI auto-deploy on push) + UAT (`auto_deploy.sh admin-node UAT`) 2026-07-13.
- **Backfill note:** pre-fix async-created rows have `contact_number = NULL` even though the roster (`student_lists.students_data`) has the phone (Role_Based persists its roster; Custom does not). Backfilled via `UPDATE assessment_assigned_students … FROM jsonb_array_elements(student_lists.students_data) WHERE contact_number IS NULL` matched by email — safe to re-run, idempotent (only fills NULLs).
- **Add-to-existing-assessment dropped the phone entirely — no SMS OTP for added candidates (fixed 2026-07-22).** Adding a candidate to an *already-created* corporate OTP campaign (e.g. a Meesho AI Interview "Customer Support" campaign) never sent an SMS OTP even with MSG91 funded, because the phone was lost on **both** sides of that path (distinct from the assign/clone paths, which were already wired):
  - **admin-node** `_addStudentsToOneTimeAssessment` (`app/models/Assessment.js`, behind `POST /assessment/addStudentsToAssessment`) hardcoded `phoneNumber: ""` when provisioning the silent account **and** its `createMany` never set `contact_number` on the assignment row. Since the `/s/` resolver mints the SMS phone claim from `COALESCE(aas.contact_number, spp.contact_number)` (`getInviteTokenPayload`), both sources were empty → the click-time token had no `phone` → UMS `requestOtp` skipped SMS. Fix: a `pickPhone(s)` helper (`s.phone || s.mobile_number || s.mobile || s.contact_number || s.phoneNumber`) now feeds **both** the assignment row's `contactNumber` (load-bearing) and the provisioned profile's `phoneNumber`.
  - **admin-react** `CorporateAssessmentDashboard.js` (`handleAddCandidateSubmit`) rebuilt each `bulkUploadData` row with only `first_name/last_name/email/primary_email`, **dropping `c.phone`** that the `EnhancedBulkUploadDrawer` captured. So the mobile never even left the browser for the corporate add flow. Fix: forward it as `mobile_number: c.phone || c.mobile_number || c.mobile`. (The institute `AssessmentDetails` add path already spread `...candidate`, so it carried the phone; institute is portal-login anyway so this is display-only there.)
  - Deployed DEV + UAT 2026-07-22. **Forward-only** — populates `contact_number` for new adds; pre-fix rows aren't backfillable (add-to-existing does not persist a roster). Verified on PROD that all 17 Meesho "Customer Support" candidates from a prior add had empty `contact_number`/`spp.contact_number` (the "shared encrypted phone" decrypts to `""`), confirming the root cause was this missing thread, not a decryption artifact.
- **NB on MSG91 balance:** the legacy `balance.php?type=1/4` API reads `0` for account `pluginlive5` even when funded — it reports the *old route* wallets, not the wallet the Flow/OTP platform draws from. Check the actual wallet in the MSG91 dashboard (Wallet balance / "Send OTP" SMS count) instead. The Flow API also returns `{"type":"success"}` at zero balance (queued ≠ delivered), so a `success` response is not proof of delivery.
- **Still not sourced:** the drop-off **resend** reminder path (`resendInvitesToStudents`) — lower priority; falls back to the user-record lookup.
- **`SMS_ENABLED` master switch:** only the literal string `"true"` enables SMS; anything else (unset / `"false"`) skips the lookup+send entirely and keeps the flow email-only. Read per-request, so it can be flipped by env without a redeploy.
- **Response additions (backward compatible):** `/request` & `/resend` now also return `sms_sent: boolean` (a phone was on file and an SMS was dispatched — **not** a delivery guarantee) and `masked_phone: string|null` (last-4 masked, e.g. `••••1234`; full number never returned).
- **Frontend hint:** `Assessment-React` `InviteStart` OTP screen appends "and texted to **••••1234**" to the "Code sent to &lt;email&gt;" line **only** when `sms_sent` is true; email-only candidates see the unchanged copy.
- **DLT is mandatory for delivery (external, not code).** MSG91's Flow API returns `type:"success"` on **acceptance into the queue** — that does **not** mean delivered. Indian carriers reject via DLT unless (1) the SMS template is **DLT-approved** (MSG91 shows "Verified by DLT"; template `6a4deb0448d912e6610658e3`, sender **PILTEC**, DLT template `1707167757323433650`) **and** (2) the **PE-TM chain** is Active on the DLT operator portal — for MSG91 the telemarketer is **Walkover Web Solutions**, TM ID **`1302157225275643280`** (added via the DLT portal's "Telemarketer Request", accepted by MSG91, then chain approved). A missing chain surfaces in MSG91 Delivery logs as **"PE-TM Chain Error on DLT"**. Both were completed for `pluginlive5` on 2026-07-09.

### SMS env keys (`user-management-node/.env.<env>`, box-local, NOT in git)

| Key | Value | Notes |
|---|---|---|
| `MSG91_AUTHKEY` | (account authkey) | shared with WhatsApp; already present |
| `MSG91_SMS_OTP_TEMPLATE_ID` | `6a4deb0448d912e6610658e3` | DLT-approved OTP Flow template |
| `SMS_ENABLED` | `"true"` | `"true"` sends; anything else = email-only |
| `MSG91_FLOW_URL` | *(optional)* | defaults to `https://control.msg91.com/api/v5/flow/` |

DEV + UAT: live 2026-07-09 (`SMS_ENABLED="true"`). PROD: pending (set the keys + `SMS_ENABLED` and confirm the DLT chain before enabling).

## Invite link shortener (`/s/<code>`) — 2026-07-15

The invite URL used to carry the whole JWT in its path (~475 chars): wrapped and broken by mail clients, unusable in WhatsApp templates, impossible to read aloud. It is now a **49-char** `https://assessment.<env>.pluginlive.com/s/<9-char code>`.

```
GET /s/vMkS1k3jc            (admin-node, public, unauthenticated)
  -> resolve code -> assessment_assigned_id      (reject if unknown / revoked / expired)
  -> re-derive email, type, role, company, phone from the assignment
  -> mint invite JWT, TTL = min(24h, time left on the link)
  -> 302 -> /assessment/start/<jwt>              (FE route unchanged)
```

The row stores **only the assignment id**, and this is load-bearing rather than incidental:

- **No PII anywhere.** A JWT is signed, not encrypted — the old URL exposed the candidate's email, phone, role and company to every log, scanner and browser history that saw it.
- **Secret rotation can't orphan links.** Rotating `ASSESSMENT_INVITE_SECRET` would have invalidated every stored JWT.
- **It fixes a live bug.** A token frozen at send time keeps a typo'd email forever and 403s the candidate off their own invite (`requestOtp` matches on the email claim). Deriving at click time means correcting the email **repairs** the existing link.
- **Links are revocable and expirable for the first time** — the deadline moved from an unrevocable JWT to a DB row. Re-issuing a revoked link **rotates** to a fresh code rather than resurrecting the dead one.

Codes are 9 chars over `[a-z][A-Z][0-9]` (62^9 ≈ 1.4e16) from `crypto.randomInt` (rejection-sampled — a `randomBytes % 62` shortcut would skew toward the first 8 letters since 256 % 62 ≠ 0). The code is the credential, so the endpoint is public by design — the **OTP gate behind it** is what authenticates.

### Concurrency — the two unique constraints mean opposite things (2026-07-15)

Nothing is pre-checked with a `SELECT` (that narrows the race window instead of closing it). The DB decides and `classifyInsertError()` says what its answer means:

| Constraint hit | Verdict | Why |
|---|---|---|
| `code` (PK) | **retry** with a new code | A genuine code collision. Effectively impossible at 62^9, but the index is the only thing that can prove it. **This is what makes "two requests can never get the same URL" true** — the PK physically forbids one code against two assignments. |
| `assessment_assigned_id` (UNIQUE) | **adopt** the winner's code | Not an error. `getOrCreateCode` is find-then-insert and **not atomic**, so N in-flight sends/copies for ONE assignment all miss the find and race to insert. The loser must return the winner's code — one assignment, one URL, is exactly the guarantee this index exists to enforce. |
| anything else | **rethrow** | FK violation, connection loss, an unmodelled constraint. Guessing "retry" would spin 5 attempts; guessing "adopt" would return an unrelated row's code. |

**This was a live bug until 2026-07-15** (fixed in `ca40f7e`, UAT `aaf02aa`). The handler originally only recognised a collision when `meta.target` was `["code"]`, so every loser of an assignment race **threw**. Measured on DEV: 20 concurrent calls for one assignment → 12 winners, **8 rejections**; 50 → 7 rejections.

Note what was and wasn't at risk: the stored data was **never** wrong — one row, one code, and two requests could never be handed the same URL. It was a **liveness** failure, and a silent one: `sendAssessmentInviteEmail` reports failure by returning `false` rather than throwing, so a losing racer's invite email simply never sent, with nothing in the logs (Copy Link surfaced it as a 500). Realistic triggers: a bulk assign overlapping a Copy Link, a double-clicked resend, an assignment-worker retry. Verified after the fix on both DEV and UAT: 20- and 50-way concurrency → 0 rejections, 1 distinct code, 1 row.

A **revoked** winner is rethrown rather than adopted or retried — the row blocking the insert is the row just read, so retrying could only burn attempts, and re-minting a link someone just killed is not the sender's call.

### Link expiry follows the assessment window — NOT a flat TTL (2026-07-15)

`expires_at` = the assessment's `end_time` (`COALESCE(assessment_corporate_map.end_time, assessment_institute_map.end_time)`) — the same instant the invite email already prints as *"Please complete the assessment by …"*. `ASSESSMENT_INVITE_TTL_DAYS` (14) is now only a **fallback** for assignments with no window set.

The old flat 14-day TTL was wrong in both directions:

- **It stranded candidates.** On DEV, **45 of 130** corporate assessments (~35%) run a window longer than 14 days — longest **77 days**. On those, the email promised a 60-day deadline while the link died on day 14; the candidate hit *"Invalid or expired invite link"* on a wide-open assessment. This predated the shortener (the JWT was minted at send with `expiresIn: 14d`); the workaround was invisible because support just resent the invite, minting a fresh token.
- **It kept dead links alive.** A closed assessment still handed out a working 14-day link. The row is now written already-expired, so `resolveCode` rejects it.

Anchoring to `end_time` also makes the deadline **idempotent** — it no longer re-opens a fresh 14 days on every send/copy. Extending an assessment's `end_time` propagates to the existing link on the next send/copy (the resolver reads the stored `expires_at`, not `end_time` live — so *shortening* a window leaves existing links on the old deadline until re-copied).

### The 24h click ticket

The click-minted JWT is capped at **24h** (`ASSESSMENT_INVITE_CLICK_TTL_HOURS`) **and** by the time left on the link. This is what makes the above safe: the clamp used to be the link's whole remaining window, so a link running to a far-future `end_time` would mint a bearer JWT of the same length. **Not hypothetical** — institute assignments carry `end_time` in **2036**, so a click would have minted a ~10-year token (verified on UAT: the clamp returns 24.00h, not 87,648h).

A ticket only has to survive the OTP ceremony (click → request code → go read the email → type it in). `verifyOtp` then issues the scoped `assessment:run` token the attempt actually runs on, so **nothing mid-attempt depends on the invite token** — raising it would not fix a mid-assessment 401 (see the scoped-JWT and portal-header gotchas below for what actually does). Floored at 1s: `resolveCode` only guarantees `msRemaining > 0`, and `jwt.sign({expiresIn: 0})` mints an already-dead token.

### Deploy order — the table and nginx MUST land before admin-node

`buildInviteUrl` now hits the DB, and it is called **inside** `sendAssessmentInviteEmail`'s try block, which reports failure by returning `false` rather than throwing. Consequences if admin-node ships to an env that isn't prepared:

- **No table** → every new invite email **silently stops sending** (no exception surfaces). Copy Link is louder: HTTP 500.
- **No nginx `location /s/`** → invites send fine but every link 404s into the React SPA, while old JWT links keep working — so it looks like only new candidates are affected.

Required per env, in order: (1) the DB-Scripts migration, (2) `location /s/` → admin-node `:8000` on the **assessment-react** vhost, placed **above `location /`** so the SPA doesn't swallow it, plus the `invite_shortlink` `limit_req_zone` in `conf.d/`, (3) then deploy admin-node.

The rate limit is deliberately generous — **10r/s, burst 200**. A tight per-IP limit is actively dangerous: campus drives put a whole college behind one NAT egress IP, so a strict limit would 429 real students mid-drive. At 62^9 the keyspace already makes guessing infeasible; the zone only exists to stop a scanner exhausting the DB.

**PROD routing is a K8s Ingress, not an nginx vhost — and it was the 2026-07-18 incident.** PROD (OKE) has no `assessment-react` nginx file; the host `assessment.pluginlive.com` is served by `assessment-react-ingress` (namespace `frontend`, path `/` → `assessment-react`). admin-node had shipped to PROD with the code, table, env and secret chain all correct, so PROD was **generating and sending `/s/` links** — but with no `/s/` route, every click fell into the SPA and rendered the **login page** (a corporate AI-Interview OTP invite that "redirects to login" is this exact symptom). Fixed by a **separate** Ingress `assessment-shortlink` (namespace `api`, so it can reference the `admin-node` service directly — ingress backends must be same-namespace):

```yaml
# ~/pl-oks-cluster/api-ns/Ingress/assessment-shortlink-ingress.yaml
kind: Ingress            # namespace: api
spec:
  ingressClassName: nginx
  rules:
  - host: assessment.pluginlive.com
    http: { paths: [ { path: /s/, pathType: Prefix,
             backend: { service: { name: admin-node, port: { number: 80 } } } } ] }
```

ingress-nginx **merges** the two Ingresses by host (nginx longest-prefix sends `/s/…` to admin-node, everything else to the SPA). TLS is **deliberately omitted** here: the `assessment-tls` secret lives in `frontend`, and an `api`-namespace Ingress referencing it would fail to find it and fall back to a default cert — the frontend Ingress terminates TLS for the host and `/s/` is served under that same cert. No rate limit, matching the sibling `admin-api-ai-interview` (`/ai-interview/`) candidate path. Applying an Ingress is **config-only**: no pod restart, persists in etcd across restarts (`kubectl delete ingress assessment-shortlink -n api` reverts).

### Backward compatibility

Existing `/assessment/start/<jwt>` links already in inboxes **keep working** — that FE route, the JWT contract and `ASSESSMENT_INVITE_SECRET` are untouched, and nothing revokes them. They age out on their own 14-day `exp`. So for up to 14 days after a deploy an old assignment can have **two different valid links**. Note the "email and Copy Link emit the identical URL" guarantee only holds for assignments invited **after** the deploy: for a pre-deploy assignment, the candidate's inbox has the old JWT link while Copy Link now returns a `/s/` URL — both work, but they differ on a support call.

## Candidate flow

1. Candidate opens the invite link — `assessment.<env>.pluginlive.com/s/<code>`, which 302s to `/assessment/start/<token>` (pre-2026-07-15 links point straight at the latter and still work).
2. Assessment-React `InviteStart` component prompts for email → calls UMS `/public/assessment-otp/send` → 6-digit OTP delivered.
3. Candidate enters OTP → `/public/assessment-otp/verify` → UMS mints a scoped JWT (`scope: "assessment:run"`) carrying `assessment_assigned_id`, `assessment_type`, `email`, `role`, `company`. **TTL = the portal login token lifetime** (`SCOPED_JWT_TTL = server.LOGIN_TOKEN_EXPIRES_IN`, currently `43200000` → parsed by jsonwebtoken's `ms` as 43.2M **ms** = **~12h**). Previously this was hardcoded to `"2h"`, which expired mid-attempt on long assessments (Communication/Hinglish with video upload) and surfaced as a `401` on **every** authed call — `heartbeat`, `saveResponse`, `uploadVideo`, `submitAssessment` — with a "Failed to submit assessment: Request failed with status code 401" alert. Bumped to match login 2026-06-15 (sourced from the same config so the two stay in sync). The scoped token has no refresh; an attempt still must complete within the TTL window from OTP verification.
4. `student-node` `/ai-interview/invite/resolve` (generic despite the legacy path name) reads the corporate map + assessment row, returns the **real assessment name**, the type-specific runner config, both proctoring/verification flags (**`allowProctoring` + `allowVerification`** — the latter added 2026-07-17, see gotcha below), and the assignment's **`status` + `submitted`** (added 2026-06-15 — see "already-submitted gate" below).
5. Assessment-React `InviteAssessmentRunner` dispatches to the matching `*assmt` partial (Communication, Aptitude, Role-Based, Behaviour, Custom, AIInterview). **`Hinglish` is folded onto the Communication runner** (2026-07-03) — it is not a real type, just Communication with `response_language=Hinglish`, so it must not mount the legacy `Hinglishassmt` fork (see gotcha). The runner title shows the real assessment name (not "AI Interview").
6. On completion, the candidate **stays on the terminal Thank-You screen** ("You can now close this window"). There is **no post-completion redirect** — the OTP `sessionStorage` is left intact. (Previously a "Back to Home" button cleared the scoped JWT and hard-redirected to `/assessment`; with the JWT gone `AuthRouter` fell through to `AuthPage`, which auto-fires `window.open(AUTH_URL)` on mount — so finishing an invite interview bounced the candidate to the portal login page. Fixed 2026-06-15 by removing the button.)

### Already-submitted gate (re-login no longer reopens a finished interview)

A candidate who re-opens their invite link (or re-verifies OTP) after submitting must **not** be put back into the interview. As of 2026-06-15:
- `resolveInvite` returns `status` (`PENDING`/`INPROGRESS`/`COMPLETED`/`DROPOUT`) and `submitted` from `assessment.assessment_assigned_students`.
- `InviteStart` checks these on mount: when `status === 'COMPLETED'` (or `submitted === true`) it renders an **"Interview already submitted"** screen instead of the email/OTP wall.
- Server-side backstop: `student-node` `startSession` returns **409** if the assignment is already `COMPLETED`/`submitted` (a fresh `AIInterviewSession` is never created). `INPROGRESS` resumes are unaffected.

## Backend touchpoints

| Repo | What changed |
|---|---|
| `admin-react` | Checkbox in Assessment Assignment form (corporate only, entity-select step); `isOtpInvite` carried through `filterAssessmentData` (the function used to strip it) for the direct Send flow. **For Role-Based** (which is filtered OUT of the Send flow and uses Generate Test → Review Panel), the same `isOtpInvite` state is passed as a prop from `CreateAssessment` into `AssessmentSelect`, which adds `isOtpInvite` to the `initiateRoleBasedGeneration` payload (corporate only) — wired 2026-06-15. The Review-Panel `assignStudentsToGroup(groupId)` call sends only `groupId`, so the flag MUST be captured at generation time. |
| `admin-node` | `assignAssessmentSchema` accepts `isOtpInvite` (was stripping it via the Fastify body schema); `assign*Assessment` methods set it on the corporate map and gate email branches via `isCorporateOtpInvite`. Invite URL built from `ASSESSMENT_FE_BASE_URL` env var. **Per-type wiring is independent** — each `assign*Assessment` method must thread `isOtpInvite` and add an OTP branch separately. Communication/Hinglish/Aptitude/Custom/AI_Interview were wired first (2026-06-10); **Role-Based + Behaviour wired 2026-06-15** (`assignRoleBasedAssessment`→`createRoleBasedAssessment` in `script/generateRoleBasedQuestions.js`, and `assignBehaviorAssessment`). Before that, ticking the OTP checkbox on a Role-Based/Behaviour corporate assign was silently ignored and the candidate got the legacy portal-signup/reminder email instead of a scoped `/assessment/start` link. **Role-Based has TWO assign paths and admin-react uses the OTHER one** — see the set-group gotcha below; the `set-group` path was wired for OTP on **2026-06-15** (later same day). |
| `admin-node` (Role-Based set-group flow) | The path admin-react actually drives for Role-Based is **Generate Test → Review Panel → Assign**, NOT the direct `assignRoleBasedAssessment`. Wiring (2026-06-15): `initiateRoleBasedGeneration` handler persists `isOtpInvite` into `groupConfig` (corporate only); `AssessmentSetGroupService.assignStudents` reads `config.isOtpInvite`, sets `isOtpInvite` on the `assessmentCorporateMap` create, and passes it to `processStudentsForAssessment`; that function (in `script/generateRoleBasedQuestions.js`) captures each `assessmentAssignedStudent.id` in STEP 1 and, when `isOtpInvite && corporate`, short-circuits STEP 2 — sending `sendAssessmentInviteEmail({assessmentType:'Role_Based', role, company, ...})` to every candidate and **skipping account creation + portal reminders**. The flag is decided at *generation* time (the Generate Test confirm dialog), stored on the group, and applied at *assign* time. |
| `admin-node` (invite shortener, 2026-07-15) | `app/service/InviteShortLinkService.js` — code generation (`crypto.randomInt` over base62), `getOrCreateCode` (deadline from the assessment window; find-then-insert, so races are resolved by `classifyInsertError` — see Concurrency above), `computeLinkExpiry`, `clickTokenTtlSeconds`, `classifyInsertError`, `resolveCode`, `recordClick`. `app/helpers/assessmentInviteEmail.js` — `buildInviteUrl` is now **async** and returns `/s/<code>`; `mintInviteToken` gained `expiresInSeconds`; `buildStartUrl` builds the 302 target (absolute — `/s/` is reachable on two hosts, so a relative `Location` would resolve against the admin API origin). `app/handlers/assessmentHandler.js` — `resolveInviteShortLink`. `app/routes/assessment.js` — public `GET /s/:code`. `app/models/Assessment.js` — `getInviteTokenPayload` (shared by Copy Link and the resolver so they cannot drift). Prisma model `InviteShortLink` in `schema-assessment.prisma`. Tests: `test/inviteShortLink.spec.js` (33). |
| `user-management-node` | Renamed handler/model/routes; backward-compatible aliases retained; scoped JWT carries `assessment_type`. Scoped JWT TTL sourced from `LOGIN_TOKEN_EXPIRES_IN` (matches login, ~12h) — was hardcoded `"2h"`. The `assessmentRemainder` handler (`app/handlers/user.js`) selects the email template by flag: `isPracticeAccess && !isCorporate` → practice reminder; `isCorporate` → `assessmentStudentCorporate2` (login **email + password**, auto-creates the user + `temp_password` if missing); else → institute reminder. It does NOT send OTP links — OTP campaigns must use `sendAssessmentInviteEmail` in admin-node instead. |
| `student-node` | `resolveInvite` reads the real campaign name from the corporate/institute map and **no longer hard-codes `"AI Interview"`** for the title — that was the early bug where Communication invites displayed as "AI Interview". Also returns `status` + `submitted` so the FE can gate an already-submitted invite, and `startSession` rejects a completed assignment with 409 (both 2026-06-15). Exposes `POST /students/assessments/invite/reloadGuard` for the runner reload→dropout guard (see gotcha). |
| `Assessment-React` | `InviteStart` is generic; `InviteAssessmentRunner` dispatches by `assessment_type` via a `TYPE_MAP` + `switch` — **a type with no entry renders "Unsupported assessment"**, so every OTP-capable type must be listed there. Cases: `Communication`, `Aptitude`, `Custom_Assessment`, `Behavior`, `Role_Based` (last added 2026-06-15). **`TYPE_MAP.Hinglish` now resolves to `'Communication'`** (2026-07-03), so a `Hinglish` token opens the Communication runner and the standalone `Hinglish` case/import were removed. Exact token `assessmentType` claim strings (underscores, US spelling): **`Role_Based`**, **`Behavior`** — not `Role-Based`/`Behaviour`. Cognitive/Tech_MCQ/Tech_CODING have no invite runner, so OTP can't run end-to-end for them. |
| `Assessment-React` (cont.) | the OTP input is a fixed-width 48 px box grid (the earlier overflow bug); demo OTP code hint removed. `InviteStart` gates `status==='COMPLETED'` to an "already submitted" screen. On the "Verify your email" (email/OTP) screen the **"Applying for &lt;role · company&gt;" subtitle now renders only when `invite.assessmentType === 'AI_Interview'`** (2026-07-24) — since `InviteStart` is shared across all OTP types, it previously showed that line for Communication/Aptitude/Hinglish/Custom/Behavior/Role_Based invites too (e.g. "Applying for Communication 24/07"); only AI-Interview invites carry a meaningful role/company context. **AI-interview completion is terminal — no "Back to Home" / redirect** (removing it fixed the bounce-to-auth, 2026-06-15). The invite axios layer (`aiInterviewInviteAPI.js`) **reuses the shared `utils/authRequest` + `utils/studentRequest` instances** — see the env gotcha below. |

## Environments

| Env | Status | `ASSESSMENT_FE_BASE_URL` |
|---|---|---|
| DEV | live since 2026-06-10 | `https://assessment.dev.pluginlive.com` (or unset — hardcoded fallback covers DEV by accident) |
| UAT | live since 2026-06-11 | `https://assessment.uat.pluginlive.com` — must be set in `~/api/admin-node/.env.uat` |
| PROD | pending | `https://assessment.pluginlive.com` — must be set before sending live invites |

### Shortener rollout (`/s/<code>`)

| Env | Table | nginx `/s/` | admin-node | Status |
|---|---|---|---|---|
| DEV | applied 2026-07-15 | `sites-available/assessment-react.conf` | `ca40f7e` | live |
| UAT | applied 2026-07-15 | `sites-available/assessment-react.conf` + `conf.d/invite-shortlink-limit.conf` | `aaf02aa` (UAT) | live 2026-07-15 |
| PROD | **pending** | **pending** | pending | not rolled out — see deploy-order note above |

## Backward compatibility checklist

- Existing AI Interview invites continue to work via the `/public/interview-otp/*` and `aiInterviewInviteOtp` aliases.
- Every existing corporate campaign has `is_otp_invite = false` after backfill (NOT NULL DEFAULT false), so portal-signup + reminder behavior is unchanged for them.
- Institute campaigns are untouched — the column does not exist on `assessment_institute_map`.
- **AI Interview is the special case — no `is_otp_invite` checkbox.** Unlike the other corporate types (which are OTP only when the admin ticks the box, gated via `is_otp_invite`), AI Interview never had the flag and was **unconditionally OTP for every assignment, corporate *and* institute**. As of **2026-06-17** `assignAIInterviewAssessment` branches on `isCollege`: **corporate → OTP invite** (unchanged), **institute → normal student-portal flow, no OTP** (new students get the activation email only; existing students get the assessment reminder via `sendRemindersToStudents` only — split by new-vs-existing like Communication). See [ai-interview.md](ai-interview.md) "Invite delivery splits by entity type". So institute AI Interview candidates now use the portal like any other institute assessment.

## Known gotchas (carried over from build)

- **`ASSESSMENT_FE_BASE_URL` env var.** Without it, the admin-node helper falls back to the DEV URL — UAT and PROD admin-node `.env.<env>` files must include the matching frontend base URL or every invite email links candidates to DEV.
- **Don't verify the base URL with a bare `docker exec admin node -e ...`.** The var is loaded by `dotenv` from the image's `.env` at app startup, so it is **not** in the container's process environment: `docker exec admin printenv` won't show it, and a fresh `node -e` that skips `require('dotenv').config()` reads `undefined` → the helper falls back to the DEV URL and the probe **looks like a live DEV-leak that isn't one**. Use `docker exec -w /app admin node -e "require('dotenv').config(); ..."`, or just curl the real `/s/<code>` and read the `Location` header.
- **A raw `/s/` probe returning `text/html` means nginx is missing the route** (the SPA swallowed it); `application/json` means it reached admin-node. A bogus code correctly 302s to `/assessment/start/invalid`, which renders the FE's existing "Invite link invalid" screen — there is no dedicated expired page, so *"expired"* and *"never existed"* look identical to the candidate (deliberate; upgrade path is a real `/s/expired` FE route).
- **The invite JWT cannot strand a mid-assessment candidate**, so don't raise its TTL to chase a mid-attempt 401. It is used for exactly three calls — `resolveInvite`, `requestOtp`, `verifyOtp` — and only in `InviteStart.js`; the runner's calls carry the **scoped** `assessment:run` token instead. A mid-attempt 401 is one of: the scoped token aged out (12h from OTP verify — very common for QA leaving a tab open), a stale portal token shadowing the scoped one, or an `authRequest` 401 triggering the global session-clear. See the three gotchas below.
- **Re-clicking the invite link mid-OTP invalidates the code you were sent.** `requestOtp`/`verifyOtp` look the OTP row up by the **exact `invite_token` string**, and the shortener mints a *new* token on every click. Within one page session this is fine (both calls use the token from the URL). But clicking the link again after requesting a code yields a different token, so the old OTP row no longer matches. Self-correcting in practice — the OTP field only appears after a successful `requestOtp`, so the re-click forces a fresh code — but a candidate holding two OTP emails who types the older one gets *"Incorrect code."* Side effect: a re-click also bypasses the `RESEND_WINDOW_MS` cooldown, since that is keyed on `(email, invite_token)`.
- **Fastify body schema strips unknown fields.** `admin-node` `assignAssessmentSchema` must explicitly list `isOtpInvite` or the handler never sees it (server logs the row with `is_otp_invite = false` even though the request carried `true`). Same applies to any future toggles added on this form.
- **`admin-react` `filterAssessmentData`** rebuilds the payload from a whitelist before submit. Any new field on the form must be added there or it silently disappears.
- **Title is dynamic, not hard-coded.** `resolveInvite` returns the campaign's real assessment name; the runner uses it for the instructions header. Old caches on the client can still show "AI Interview" — a fresh OTP verify rewrites the `sessionStorage` entry, so retesting requires reopening the invite link.
- **Per-type OTP wiring is not automatic.** A new corporate assessment type does NOT get OTP invite for free: its `assign*Assessment` method must (1) thread `isOtpInvite`, (2) set `isOtpInvite` on the corporate map create, (3) skip account creation + portal/reminder emails when on, (4) send `sendAssessmentInviteEmail` per candidate with the correct `assessmentType` claim, AND the FE `InviteAssessmentRunner` `TYPE_MAP`/`switch` must include that type string — otherwise the candidate hits "Unsupported assessment".
- **Resend-invite / send-reminder must respect the campaign mode (fixed 2026-06-17).** The admin drop-off **resend** (`/assessment/resendInvites` → `Assessment.resendInvitesToStudents`) and **reminder** (`/assessment/sendReminders` → `sendRemindersToStudents`) in `admin-node` originally sent the wrong email for corporate: (a) for OTP campaigns they sent a portal reminder instead of the scoped OTP link, and (b) `resendInvitesToStudents` omitted `isCorporate` in the `resendAssessmentInvite` payload, so the UMS `assessmentRemainder` handler fell through to the **institute-reminder** template instead of the corporate **login email+password** template (`assessmentStudentCorporate2`). Both methods now read `assessment_corporate_map.is_otp_invite` and branch: corporate+OTP → `sendAssessmentInviteEmail` (scoped `/assessment/start` link, `assessmentType` = DB `type_name`); corporate+portal → reminder with `isCorporate:true` (UMS sends email+password); college → institute reminder. NB: `resendInvitesToStudents` also **resets** the dropped assignments (new set, deletes old answers/scores/proctoring) before emailing — never invoke it as a dry test.
- **Add-students-to-existing campaign must respect the mode too (fixed 2026-06-19).** Adding candidates to an already-created campaign (`/assessment/addStudentsToAssessment` → `Assessment.addStudentsToExistingAssessment` → `_addStudentsToOneTimeAssessment`) was OTP-unaware: it always created a portal student account (so `createPublicStudent` sent the activation/credentials email) **and** sent `sendRemindersToStudents` to pre-existing candidates — never the scoped OTP link. So adding a student to an existing corporate OTP campaign (e.g. AI Interview) emailed **login credentials instead of the OTP invite**. Now the one-time add flow detects OTP via the shared predicate **`admin-node/app/helpers/otpInviteCampaign.js` → `isCorporateOtpInviteCampaign({isCorporate, isOtpInvite, assessmentTypeName})`** = `isCorporate && (is_otp_invite || assessmentTypeName === 'AI_Interview')` — AI Interview corporate is always OTP yet keeps `is_otp_invite=false`, so the flag alone is insufficient. When OTP: new accounts are created **silently** (`skipActivationEmail: true`) and `sendAssessmentInviteEmail` (scoped link, `assessmentType` = DB `type_name`, `role:null`) is sent to **every** added candidate — both brand-new and pre-existing; the portal reminder is skipped. Non-OTP corporate and all institute flows are unchanged. This predicate is the single source of truth shared by the assign/resend/add paths; any future add/clone path should call it rather than re-deriving the condition.
- **Reload of an in-progress invite attempt = DROPOUT (fixed 2026-06-17).** The invite runner (`Assessment-React` `InviteAssessmentRunner`, route `/assessment/take/:id`) re-mounts on a browser reload / re-entry and used to re-show the instruction page, letting a candidate restart an attempt they had already begun. On mount it now calls `student-node` **`POST /students/assessments/invite/reloadGuard`** (public, like `getAssessmentQuestions`), which returns a `state`: `pending`/`resume` → render instructions; `completed` → "Already submitted"; `dropout` → "Assessment closed" terminal screen. For a non-resumable attempt already `INPROGRESS` the endpoint **atomically marks it `DROPOUT`**, so a reload cannot be used to retake. `is_practice` is never locked; diagnosis (institute map, no `scheduleId`) stays resumable. Applies to all OTP runner types (Communication/Hinglish/Aptitude/Custom/Behavior/Role_Based). AI Interview keeps its own resume flow (its runner is `/ai-interview/:id`, not this one). The FE guard fails open (transient error → `pending`), and the existing start-guard in `getAssessmentQuestions` (`RESUME_NOT_ALLOWED`/`ALREADY_IN_PROGRESS` 409) still backstops the actual question fetch.
- **Role-Based has TWO assign code paths — admin-react uses the set-group one.** (1) The direct `assignRoleBasedAssessment` → `createRoleBasedAssessment` path (wired for OTP earlier 2026-06-15) is **NOT** what the admin UI calls. (2) admin-react filters Role-Based out of the direct "Send" flow and routes it through **Generate Test (`initiateRoleBasedGeneration`) → Review Panel → `assignStudentsToGroup` → `AssessmentSetGroupService.assignStudents` → `processStudentsForAssessment`**. Wiring only the first path looks correct in code review but does nothing for real assignments. The set-group path stores `isOtpInvite` in `assessment_set_groups.group_config` at generation time (the assign call only carries `groupId`). If OTP "doesn't work" for corp Role-Based, check `group_config->>'isOtpInvite'` on the group, not the direct handler.
- **Scoped JWT TTL = login token lifetime.** `SCOPED_JWT_TTL = server.LOGIN_TOKEN_EXPIRES_IN` in `assessmentInviteOtpHandler.js`. If `LOGIN_TOKEN_EXPIRES_IN` is **unset** in a given env, the value resolves to `undefined` → jwt.sign mints a **non-expiring** token; every env's UMS `.env` must keep this var set (UAT/DEV = `43200000` → ~12h). It must be a *string* (env vars always are) so jsonwebtoken's `ms` parses it as milliseconds; a bare number would be treated as seconds.
- **Frontend `process.env` in `aiInterviewInviteAPI.js` — TWO opposite failure modes (resolved 2026-06-15).** The invite *email link* was always correct (`assessment.uat...`), but after landing the invite/AI-interview flow misbehaved depending on how the module read its axios base URLs:
  1. **Member access** `const API_URL = process.env.API_URL` with `|| 'https://api-auth.dev.pluginlive.com/'` / `|| '...api-std.dev...'` fallbacks → in this webpack build the member-access token was **not inlined**, so `API_URL`/`STD_API_URL` were `undefined` at runtime and the **DEV fallback fired → candidates bounced to DEV** (page loaded fine, wrong env). The dev string surviving in the bundle (not folded away by terser) is the tell.
  2. **Named-key destructuring** `const { API_URL, STD_API_URL } = process.env` → URLs **did** inline correctly (no DEV leak), BUT webpack left a **dangling `process.env;` expression statement** that terser did not eliminate. At runtime `process` is undefined → **`Uncaught ReferenceError: process is not defined` → blank screen** on the invite entry page. (The tiny `src/utils/*Request.js` files use this same destructuring safely because the binding is their only statement and the declaration gets fully DCE'd; a larger module like this one keeps the dangling reference.)
  - **Final fix:** do **not** touch `process.env` in this chunk at all. Reuse the shared, already-configured axios instances: `import authRequest from 'utils/authRequest'` (→ `API_URL` / api-auth / UMS, for the public OTP endpoints) and `import studentRequest from 'utils/studentRequest'` (→ `STD_API_URL` / api-std, which also attaches the invite scoped JWT from `sessionStorage` that the resolve call needs). Their base URLs inline cleanly and no `process.env` reference is left in the module.
  - **Build/verify rules:** frontend env URLs are **baked at build time** (`EnvironmentPlugin` from `.env.<env>`), so **always rebuild the FE on the target box from the target-env checkout — never ship a DEV-built bundle to UAT/PROD.** After building, before swapping the container: `docker exec <c> sh -c "grep -rho '[a-z-]*\.dev\.pluginlive\.com' /app/build | sort | uniq -c"` must be empty, and a quick headless load (`playwright-core` + `/usr/bin/chromium-browser`) of `/assessment/start/<anytoken>` must show **no `pageerror`** and a non-empty `#root` (a bogus token correctly renders "Invite link invalid", not a blank screen).
- **Scoring must tolerate the missing student profile (fixed 2026-06-17).** Because an OTP campaign never creates a student account, the score-calculation cron's name lookup `getFullName(primaryEmail)` throws `"Student not found"` and the assignment ends up `calculation_error=true` with no scores — the symptom users report as "scores not calculating" for a Communication/Role-Based/Behaviour OTP invite. All scoring entry points in `student-node/app/models/Assessment.js` now go through **`getFullNameForScoring(assessment_assigned_id, primaryEmail)`**, which derives a name from the email local-part when the assignment is `ai_interview` **or** `is_otp_invite=true`, and only rethrows for registered-candidate flows. Any NEW scoring path or assessment type must use this helper, not `getFullName` directly. To re-score rows stuck by the old behaviour: set `scores_calculated/is_processing/calculation_error` to false and `calculation_attempts=0`; the cron re-picks within ~1 min. See [ai-interview.md](ai-interview.md) "corporate candidates with no student profile".
- **`createPublicStudent` is now idempotent end-to-end (fixed 2026-06-30).** `student-node` `app/handlers/common.js#createPublicStudent` short-circuits with `{id}` (HTTP 200) the moment it finds a non-deleted student for `admin.email`. Pairs with the **admin-side** guard (`db.studentExistsByEmail` + `isDuplicateEmailError`) that already covered the provision stage, so a slow-but-successful create that the gateway 504'd no longer loops through the full pipeline (cross-service role/institute lookups + a blind insert) on retry. Net effect: stuck `provisioning` queue items from a UAT roleMetrics event-loop storm self-heal on the next worker retry instead of burning all `createPublicStudent` attempts. The full root cause of the storm (PUT `/students/roleMetrics/bulkUpdate/trigger` ~78×/15min, 33s each, blocking the Node event loop) is separate — see [[Assessment/assignment-calculation-queue]] for the throttle work.
- **Report generation has the SAME requirement (fixed 2026-06-17).** `BehaviorCalculations.getStudentInformation(studentId)` also threw `"Student not found"` for OTP candidates, breaking all four PDF report generators (`generateCommunicationReport`, `generateRoleBasedReport`, `generateBehaviorPDFReport`, `generateAptitudePDFReport` → `generatePDFReport`). They now go through **`getStudentInformationForReport(assessment_assigned_id, student_id)`**, which returns a minimal student-info object (name derived from the email, rest null) for `ai_interview`/`is_otp_invite` assignments and rethrows otherwise. NB: the proctoring snapshot-processing / validity cron (`processPendingProctoring`, `detectFaces`, `finalizeProctoringValidation`) keys off `assessment_assigned_id` and has **no** profile dependency — it was already healthy; the visible "proctoring not calculating" symptom for OTP candidates was this report failure, not the proctoring pipeline.
- **Scoped invite token must out-rank the portal default Authorization header (fixed 2026-06-11).** The runner's data calls all go through the shared `studentRequest` axios instance. When *any* portal session exists in that browser, `utils/initializeApp.js` sets `studentRequest.defaults.headers.common.Authorization` to the portal token. The interceptor originally injected the scoped OTP token only when *no* auth header was present (`hasAuth` checked the defaults too), so the **stale/expired portal default token shadowed the fresh 12h scoped token** → OTP assessment calls failed with `jwt expired` even though a valid scoped token was sitting in `sessionStorage`. This is the "login works but OTP expires" symptom (it bites when testing the invite link in a browser that's also logged into the portal). Fix: in an active invite session the scoped token (`assessment_invite_scoped_jwt` → legacy `ai_interview_scoped_jwt`) takes **precedence** over the portal default header; only an explicit per-request `Authorization` still wins. Normal portal students never have the scoped key set (written on OTP verify, cleared on completion), so they're unaffected. The backend scoped token itself was verified healthy — minted at 12h and accepted by student-node end-to-end on DEV & UAT — so this client-side precedence was the real remaining failure, not the token lifetime.
- **Wrong OTP must show "Incorrect code.", not bounce to the email step (fixed 2026-07-01).** `verifyOtp` posts to `/public/assessment-otp/verify` via the shared `authRequest` (UMS) instance, and a wrong/expired code correctly returns **HTTP 401** `{message:"Incorrect code."}`. But `Assessment-React/src/utils/initApiServices.js` registers a **global `authRequest` response interceptor** that, on *any* 401, did `localStorage.clear() + location.reload()`. So a wrong OTP reloaded the page → `InviteStart` remounted → `resolveInvite` → back to the **"Verify your email"** stage, and the candidate never saw the error. Fix: the interceptor now **skips the session-clear/reload for `/public/` endpoints** (unauthenticated — a 401 there is a bad code/link, never an expired session) and **rejects with the original axios error** instead of `new Error(error)` (the wrapper destroyed `error.response`, so `InviteStart`'s catch couldn't read `error.response.data.message`). `InviteStart.handleVerify` already renders `err?.response?.data?.message` in its OTP-stage `<ErrorBanner>`; it just never ran because the interceptor hijacked the 401 first. NB: OTP verify lives **only** in Assessment-React (`InviteStart` + `aiInterviewInviteAPI`), not student-react — the account-activation `CheckOtpPage` flow is a separate, unrelated screen.
- **Hinglish-language Communication OTP invite must not open the legacy 20-min "Hinglish Assessment" fork (fixed 2026-07-03).** A corporate OTP Communication assign with a topic + `response_language=Hinglish` stored everything correctly — `assessment_corporate_map` and the generated `assessment_sets` row are both **Communication** type (verified on PROD, e.g. campaign "gghkgu"; the only `Hinglish`-**typed** sets/maps anywhere are pre-2026-04-22 legacy). But the candidate opened a screen titled **"Hinglish Assessment"** with a flat **20-minute** timer. Cause: `admin-node/app/models/Assessment.js:assignCommunicationAssessmentAsync` derives `inviteAssessmentType = /hinglish|hindi/.test(response_language) ? 'Hinglish' : 'Communication'` **for email branding only** (stored in the job `configSnapshot`), and the notify stage (`app/queues/assignmentWorker.js`) minted the invite JWT's `assessmentType` claim from `cfg.inviteAssessmentType` — so the token said `Hinglish`. `InviteAssessmentRunner` routed that to the legacy `Hinglishassmt` partial, whose `assessment.js` **hardcodes** the title `"Hinglish Assessment"` and `useState(20 * 60)` (a fixed 20-min cap, never derived from the set). Fix — two layers: (1) `assignmentWorker.js` now mints `assessmentType: jobRow.assessmentType || cfg.inviteAssessmentType` (the **real** type, `Communication`) so new invites route correctly; (2) `InviteAssessmentRunner` `TYPE_MAP.Hinglish → 'Communication'` so **already-sent 14-day tokens** still open the Communication runner (real section timers, real title). `inviteAssessmentType` remains for email copy only. The Hinglish *speaking* evaluation is unaffected — STT language is driven by `response_language` on the map, not by the runner. **Do not put a branding/display value in the token's `assessmentType` claim — it selects the runner.**
- **Verification (biometric) toggle was ignored on OTP invites — always showed the popup (fixed 2026-07-17).** Turning **verification off** while assigning (e.g. a corporate OTP Custom_Assessment) stored `allow_verification=false` on the map correctly, but the biometric verification popup still appeared for the candidate. Every type's instruction screen gates on `assessment?.allowVerification !== false`, so an **`undefined`** value (`undefined !== false`) keeps the popup **on**. The whole OTP-invite pipeline only ever carried `allowProctoring`, never `allowVerification` — so turning **proctoring** off worked (it's threaded end-to-end) but turning **verification** off had no effect. This was **not** Custom-specific — it hit every OTP-invite type (Aptitude/Communication/Hinglish/Custom/Behavior/Role_Based/AI_Interview); the direct/portal-login flow was fine because `student-node` `Assessment.js` already returns `allowVerification` there. Fix threads the flag through the full invite path: (1) `student-node` `resolveInvite` (`app/handlers/aiInterviewHandler.js`) now selects `acm.allow_verification`/`aim.allow_verification`, resolves it off the attached map (**default true, respect explicit false**), and returns `allowVerification` in the payload; (2) `Assessment-React` adds an `allowVerification` invite-session key (`INVITE_SESSION_KEYS` in `aiInterviewInviteAPI.js`), `InviteStart.js` writes it from the resolve payload (opt-out: on unless explicitly `false`), and `InviteAssessmentRunner.js` threads it into the `assessment` prop + `SET_CURRENT_ASSESSMENT_ID` (reducer stores it too). Deployed DEV (push to Development) + UAT (`auto_deploy.sh student-node` + `assessment-react`) 2026-07-17. **Forward-only** — the flag lives on the map, so nothing to backfill; re-open the invite link (fresh OTP verify rewrites `sessionStorage`) to retest.
- **Audio/proctoring student_id fallback.** Proctoring `verify-frame` / `detect-audio` calls fall back to `assessment_assigned_id` as `student_id` for account-less candidates. Don't gate these endpoints on a real `users.id`.
- **MediaRecorder mime gotcha.** `video/webm;codecs=vp8,opus` contains a comma; splitting on `,` for the base64 part instead of `;base64,` produces a corrupt `"opus;base64"` payload and the audio-detect endpoint returns `Incorrect padding`.
